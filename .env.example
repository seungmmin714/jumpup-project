# 통신 모드 — 코드 수정 없이 이 값만 바꾸면 전환된다
#   mock   : 하드웨어 없이 가짜 센서값 (개발·시연)
#   serial : 아두이노 Uno를 USB로 직접 연결 (Web Serial, PC 크롬)
#   ble    : HM-10/AT-09 블루투스 (Web Bluetooth, 안드로이드 크롬) — 기본값
VITE_BLE_MODE=mock

# 서버 API 기본 경로 (§12)
VITE_API_BASE=/api
